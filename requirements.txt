# ============================================================
# requirements.txt
# Strategy: pin only packages with known stable versions.
# All langchain-* integrations are unpinned so uv resolves
# the correct compatible version automatically.
# ============================================================

# --- Core Web Framework ---
django==5.2
dj-database-url==2.3.0
djangorestframework==3.15.2
django-cors-headers==4.6.0
asgiref==3.8.1
sqlparse==0.5.3
psycopg2-binary==2.9.10
djangorestframework-simplejwt==5.4.0
PyJWT==2.10.1

# --- Background Tasks & Caching ---
celery==5.4.0
redis==5.2.1
django-celery-results==2.5.1

# --- WebSocket Packages ---
channels==4.2.0
channels-redis==4.2.1
daphne==4.1.2

# --- AI & LLM Core ---
# Pin langchain + langchain-community only.
# langchain-core is intentionally unpinned — uv resolves the version
# that satisfies both langchain and langchain-community simultaneously.
langchain==0.3.25
langchain-community==0.3.24

# --- LangChain Integrations ---
# These are all unpinned — versions change frequently and uv will
# pick the latest that is compatible with langchain-core above.
langchain-google-genai
langchain-groq
langchain-huggingface
langchain-milvus
langchain-tavily
langchain-cerebras

# --- Multi-Agent Orchestration ---
langgraph

# --- Re-ranking ---
flashrank==0.2.10
rank-bm25==0.2.2

# --- Provider SDKs ---
groq
huggingface-hub

# --- Vector Databases ---
qdrant-client
pymilvus

# --- Static Files ---
# Serves Django admin CSS/JS directly from the app (no nginx needed)
whitenoise==6.9.0

# --- Environment & Utilities ---
python-dotenv==1.0.1
requests==2.32.3

# --- Email ---
django-anymail==12.0

# --- Google Auth ---
google-auth==2.38.0

# --- PDF / Document Extraction ---
pymupdf==1.25.3
llama-cloud

# --- Vercel Blob Storage ---
# vercel provides vercel.blob.BlobClient used in DocuAgent/utils/utility.py
vercel==0.5.3
vercel-blob==0.4.2

# --- Shared Models (git dependency — installed last) ---
git+https://github.com/dhruv9512/docugyan-shared-models.git@main#egg=docugyan-shared-models